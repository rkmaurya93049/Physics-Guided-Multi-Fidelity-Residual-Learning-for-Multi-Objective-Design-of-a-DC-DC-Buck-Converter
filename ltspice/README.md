# LTspice

Organize LTspice source/evidence into:

- `reference/` — common reference schematic/netlist
- `automation/` — sweep templates/configuration
- `validation/` — representative final candidate netlists/schematics
- `logs/` — measurement logs supporting manuscript values

Keep source files and logs; large generated `.raw` waveform files are normally unnecessary for the public release.
