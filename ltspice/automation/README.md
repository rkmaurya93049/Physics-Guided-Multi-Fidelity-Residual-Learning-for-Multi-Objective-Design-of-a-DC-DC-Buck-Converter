# LTspice Automation

The canonical sweep/automation notebook is:

[`../../notebooks/spice_automation.ipynb`](../../notebooks/spice_automation.ipynb)

The common source schematic used by that workflow is:

[`../reference/Draft1.asc`](../reference/Draft1.asc)

A duplicate copy of the notebook is intentionally not kept in this folder. This avoids two public files drifting apart while preserving a single source of truth for the automation implementation.
